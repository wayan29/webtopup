# Preferred public API v2 gateway. Same-origin deployments normally use /api/v2.
VITE_API_V2_URL=/api/v2

# Optional explicit base for uploaded assets. Leave empty to derive it from the API URL.
VITE_ASSET_BASE_URL=

# Legacy compatibility fallback used only when newer variables are unset.
# VITE_API_URL=/api
